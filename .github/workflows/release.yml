name: Release
on:
  push:
    branches: [main]
permissions:
  contents: write

jobs:
  build:
    name: Build Binaries
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[skip release]')"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Get version from flake.nix
        id: version
        run: |
          VERSION=$(grep 'version = ' flake.nix | head -1 | sed 's/.*version = "\([^"]*\)".*/\1/')
          echo "version=$VERSION" >>$GITHUB_OUTPUT
      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "v${{ steps.version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi
      - uses: actions/setup-go@v5
        if: steps.check_tag.outputs.exists == 'false'
        with:
          go-version-file: go.mod
      - uses: pnpm/action-setup@v4
        if: steps.check_tag.outputs.exists == 'false'
      - uses: actions/setup-node@v4
        if: steps.check_tag.outputs.exists == 'false'
        with:
          node-version: 22
          cache: pnpm
      - name: Build CSS and native binary
        if: steps.check_tag.outputs.exists == 'false'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          pnpm install
          pnpm exec tailwindcss --input internal/web/src/app.css --output internal/web/static/style.css --minify
          go build -ldflags="-s -w -X github.com/ahmedelgabri/ccpeak/internal/cmd.Version=$VERSION" -o ccpeak-native ./cmd/ccpeak/
          mkdir -p dist/completions dist/man
          ./ccpeak-native completion bash > dist/completions/ccpeak.bash
          ./ccpeak-native completion zsh > dist/completions/_ccpeak
          ./ccpeak-native completion fish > dist/completions/ccpeak.fish
          ./ccpeak-native man dist/man
          rm ccpeak-native
      - name: Cross-compile and package
        if: steps.check_tag.outputs.exists == 'false'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          LDFLAGS="-s -w -X github.com/ahmedelgabri/ccpeak/internal/cmd.Version=$VERSION"
          for target in darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
            OS="${target%/*}"
            ARCH="${target#*/}"
            DIR="ccpeak-${VERSION}-${OS}-${ARCH}"
            mkdir -p "dist/${DIR}"
            GOOS="$OS" GOARCH="$ARCH" go build -ldflags="$LDFLAGS" -o "dist/${DIR}/ccpeak" ./cmd/ccpeak/
            cp -r dist/completions "dist/${DIR}/"
            cp -r dist/man "dist/${DIR}/"
            tar -czf "dist/${DIR}.tar.gz" -C dist "$DIR"
          done
      - name: Generate checksums
        if: steps.check_tag.outputs.exists == 'false'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          cd dist
          sha256sum ccpeak-${VERSION}-*.tar.gz > "ccpeak-${VERSION}-checksums.txt"
      - uses: actions/upload-artifact@v4
        if: steps.check_tag.outputs.exists == 'false'
        with:
          name: release-assets
          path: |
            dist/ccpeak-*.tar.gz
            dist/ccpeak-*-checksums.txt
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag_exists: ${{ steps.check_tag.outputs.exists }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: needs.build.outputs.tag_exists == 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: dist
      - name: Generate changelog
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || git rev-list --max-parents=0 HEAD)
          CHANGELOG=$(git log "$PREV_TAG"..HEAD --pretty=format:"- %s" --no-merges | head -50)
          echo "changelog<<EOF" >>$GITHUB_OUTPUT
          echo "$CHANGELOG" >>$GITHUB_OUTPUT
          echo "EOF" >>$GITHUB_OUTPUT
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.build.outputs.version }}
          name: v${{ needs.build.outputs.version }}
          body: |
            ## Changes

            ${{ steps.changelog.outputs.changelog }}

            ## Installation

            ### Using Nix Flakes

            ```bash
            nix run github:${{ github.repository }}
            ```

            ### Using Homebrew

            ```bash
            brew install ahmedelgabri/ccpeak/ccpeak
            ```

            ### Manual Download

            Download the archive for your platform, extract it, and place the binary in your `$PATH`:

            ```bash
            tar xzf ccpeak-${{ needs.build.outputs.version }}-<OS>-<ARCH>.tar.gz
            cp ccpeak-${{ needs.build.outputs.version }}-<OS>-<ARCH>/ccpeak ~/.local/bin/
            ```

            Completions and man pages are included under `completions/` and `man/`.

            **Checksums:** See `ccpeak-${{ needs.build.outputs.version }}-checksums.txt`
          draft: false
          prerelease: false
          files: |
            dist/ccpeak-*.tar.gz
            dist/ccpeak-*-checksums.txt

  update-homebrew:
    name: Update Homebrew Formula
    needs: [build, release]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: dist
      - uses: actions/checkout@v4
        with:
          repository: ahmedelgabri/homebrew-ccpeak
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-ccpeak
      - name: Update formula
        env:
          VERSION: ${{ needs.build.outputs.version }}
        run: |
          SHA_DARWIN_ARM64=$(grep "darwin-arm64" dist/ccpeak-${VERSION}-checksums.txt | awk '{print $1}')
          SHA_DARWIN_AMD64=$(grep "darwin-amd64" dist/ccpeak-${VERSION}-checksums.txt | awk '{print $1}')
          SHA_LINUX_ARM64=$(grep "linux-arm64" dist/ccpeak-${VERSION}-checksums.txt | awk '{print $1}')
          SHA_LINUX_AMD64=$(grep "linux-amd64" dist/ccpeak-${VERSION}-checksums.txt | awk '{print $1}')

          cat > homebrew-ccpeak/Formula/ccpeak.rb << EOF
          class Ccpeak < Formula
            desc "Explore your Claude Code history"
            homepage "https://github.com/ahmedelgabri/ccpeak"
            version "${VERSION}"
            license "MIT"

            on_macos do
              on_arm do
                url "https://github.com/ahmedelgabri/ccpeak/releases/download/v#{version}/ccpeak-#{version}-darwin-arm64.tar.gz"
                sha256 "${SHA_DARWIN_ARM64}"
              end
              on_intel do
                url "https://github.com/ahmedelgabri/ccpeak/releases/download/v#{version}/ccpeak-#{version}-darwin-amd64.tar.gz"
                sha256 "${SHA_DARWIN_AMD64}"
              end
            end

            on_linux do
              on_arm do
                url "https://github.com/ahmedelgabri/ccpeak/releases/download/v#{version}/ccpeak-#{version}-linux-arm64.tar.gz"
                sha256 "${SHA_LINUX_ARM64}"
              end
              on_intel do
                url "https://github.com/ahmedelgabri/ccpeak/releases/download/v#{version}/ccpeak-#{version}-linux-amd64.tar.gz"
                sha256 "${SHA_LINUX_AMD64}"
              end
            end

            def install
              bin.install "ccpeak"
              bash_completion.install "completions/ccpeak.bash" => "ccpeak"
              zsh_completion.install "completions/_ccpeak"
              fish_completion.install "completions/ccpeak.fish"
              man1.install Dir["man/*.1"]
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/ccpeak --version")
            end
          end
          EOF
      - name: Commit and push
        env:
          VERSION: ${{ needs.build.outputs.version }}
        run: |
          cd homebrew-ccpeak
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/ccpeak.rb
          git diff --staged --quiet || git commit -m "ccpeak ${VERSION}"
          git push
