package index

// Relationship resolution is now handled during indexing via SQL.
// Todos are linked to sessions in indexTodos() using todoSessionRe.
// File history is linked to sessions in indexFileHistory() using conversationID.
